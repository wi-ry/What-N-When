name: Semantic Release

on:
  workflow_run:
    workflows:
      - CI
    branches:
      - main
    types:
      - completed

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  release:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    outputs:
      published: ${{ steps.semantic.outputs.new_release_published }}
      tag: ${{ steps.semantic.outputs.new_release_git_tag }}
      version: ${{ steps.semantic.outputs.new_release_version }}

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          ref: main
          fetch-depth: 0

      - name: Run semantic-release
        id: semantic
        uses: cycjimmy/semantic-release-action@v6
        with:
          extra_plugins: |
            conventional-changelog-conventionalcommits@9.3.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-and-upload:
    needs: release
    if: ${{ needs.release.outputs.published == 'true' }}
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          ref: ${{ needs.release.outputs.tag }}

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust build
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Install dependencies
        run: npm ci

      - name: Sync Tauri app version
        shell: pwsh
        run: |
          $config = Get-Content src-tauri/tauri.conf.json -Raw | ConvertFrom-Json
          $config.version = "${{ needs.release.outputs.version }}"
          $config | ConvertTo-Json -Depth 32 | Set-Content src-tauri/tauri.conf.json

      - name: Build Tauri app
        run: npm run build

      - name: Package MSIX
        shell: pwsh
        run: .github/scripts/package-msix.ps1

      - name: Prepare portable executable
        shell: pwsh
        run: |
          Copy-Item `
            "src-tauri/target/release/what-n-when.exe" `
            "src-tauri/target/release/What-N-When_${{ needs.release.outputs.version }}_x64-portable.exe"

      - name: Upload artifact to release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ needs.release.outputs.tag }}
          files: |
            src-tauri/target/release/bundle/nsis/*.exe
            src-tauri/target/release/bundle/msi/*.msi
            src-tauri/target/release/bundle/msix/*.msix
            src-tauri/target/release/What-N-When_${{ needs.release.outputs.version }}_x64-portable.exe

      - name: Setup MSStore CLI
        uses: microsoft/setup-msstore-cli@v1

      - name: Configure MSStore CLI
        run: |
          msstore reconfigure `
            --tenantId ${{ secrets.TENANT_ID }} `
            --sellerId ${{ secrets.SELLER_ID }} `
            --clientId ${{ secrets.CLIENT_ID }} `
            --clientSecret ${{ secrets.SECRET_VALUE }}

      - name: Publish to Store
        run: |
          msstore publish src-tauri/target/release/bundle/msix -id ${{ secrets.STORE_ID }} -v --uploadTimeout 300