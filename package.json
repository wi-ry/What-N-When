{
  "name": "what-n-when",
  "version": "3.2.5",
  "description": "What-N-When is a desktop widget that embeds Notion Calendar",
  "author": "Will Ryan <will@willryan.ca>",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "check": "node --check web/renderer.js && node --check web/optionsRenderer.js",
    "lint:commits": "commitlint --from HEAD~20 --to HEAD --verbose",
    "release": "semantic-release",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.3",
    "@semantic-release/changelog": "^7.0.0",
    "@semantic-release/commit-analyzer": "^13.0.1",
    "@semantic-release/git": "^11.0.1",
    "@semantic-release/github": "^12.0.9",
    "@semantic-release/npm": "^13.1.5",
    "@semantic-release/release-notes-generator": "^14.1.1",
    "@tauri-apps/cli": "^2",
    "conventional-changelog-conventionalcommits": "10.4.0",
    "husky": "^9.1.7",
    "semantic-release": "^25.0.9"
  },
  "overrides": {
    "ip-address": ">=10.3.1",
    "undici": ">=6.28.0"
  }
}
